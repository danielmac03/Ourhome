import { IsInWishlistPipe } from './is-in-wishlist.pipe';

describe('IsInWishlistPipe', () => {
  it('create an instance', () => {
    const pipe = new IsInWishlistPipe();
    expect(pipe).toBeTruthy();
  });
});
